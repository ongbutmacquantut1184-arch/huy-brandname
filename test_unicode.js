require('dotenv').config({ path: '.env.local' }); const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function t() { const r = await supabase.from('cx_customers').select('customer_id, ten_cong_ty').or('ten_cong_ty.ilike.%ti?p%'); console.log(r); } t();
